module.exports = {
  content: [
    "./src/**/*.{html,js,jsx,ts,tsx}"
  ],
  darkMode: 'class', // optional: 'media' or false
  theme: {
    extend: {
      // ...existing theme customizations if any...
    },
  },
  plugins: [
    // add plugins if needed, e.g. require('@tailwindcss/forms')
  ],
};